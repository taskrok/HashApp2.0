import React from 'react';
import HashApp from './components/AppLanding'; // Make sure this path is correct

/**
 * This is the main entry point for your entire application.
 * It now renders the HashApp component, which handles all
 * the navigation and tool displays.
 */
function App() {
  return (
    <HashApp />
  );
}

export default App;
